class GenerateProperties {
    private readonly _myValue: number;
    private _myOtherValue: string;

    // Invoke Generate menu and select Properties
    // Select members to generate and click Finish
}